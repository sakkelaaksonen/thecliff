---
permalink: /404.html
---
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="build-version" content="{{ build.version }}">
    <title>Page Not Found - {{ config.siteName }}</title>
    <link rel="stylesheet" href="/css/main.css?v={{ build.version }}">
    <link rel="icon" type="image/svg+xml" href="/favicon.svg">
    <meta name="description" content="The page you're looking for doesn't exist. Return to {{ config.siteName }} music bar website.">
    <meta name="author" content="{{ config.siteAuthor }}">
    <meta name="robots" content="noindex, nofollow">
    
    <!-- Open Graph meta tags -->
    <meta property="og:title" content="Page Not Found - {{ config.siteName }}">
    <meta property="og:description" content="The page you're looking for doesn't exist. Return to {{ config.siteName }} music bar website.">
    <meta property="og:url" content="{{ config.siteUrl.production }}/404">
    <meta property="og:type" content="website">
    
    <!-- Twitter Card meta tags -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="Page Not Found - {{ config.siteName }}">
    <meta name="twitter:description" content="The page you're looking for doesn't exist. Return to {{ config.siteName }} music bar website.">
</head>
<body class="bg-black text-white">
    <main id="main-content">
        <!-- 404 Content -->
        <section class="flex items-center justify-center px-4 pt-24 min-h-screen">
            <div class="text-center max-w-2xl mx-auto">
                <!-- Logo -->
                <div class="mb-8">
                    <h1 class="logo-heading mx-auto">
                        <span class="sr-only">{{ config.siteName | upper }}</span>
                    </h1>
                </div>

                <!-- Error Message -->
                <div class="space-y-6">
                    <h2 class="text-6xl md:text-8xl font-bold text-cliff-carmine-bright mb-4">4/4</h2>
                    <h3 class="text-2xl md:text-3xl font-semibold text-white mb-6">Page Not Found</h3>
                    <p class="text-lg text-gray-300 mb-8 leading-relaxed">
                        Looks like you've wandered off the beat. The page you're looking for doesn't exist, 
                        but the music is still playing at {{ config.siteName }}.
                    </p>

                    <!-- Action Buttons -->
                    <div class="space-y-4 md:space-y-0 md:space-x-4 md:flex md:justify-center">
                        <a 
                            href="/" 
                            class="inline-block bg-cliff-carmine hover:bg-cliff-carmine-dark focus:bg-cliff-carmine-dark text-white font-bold py-3 px-8 rounded-lg text-lg transition-colors duration-300 focus:outline-none focus:ring-2 focus:ring-cliff-carmine-light"
                        >
                            Back to Home
                        </a>
                    </div>
                </div>
            </div>
        </section>

        {% include "footer.njk" %}
    </main>
</body>
</html> 