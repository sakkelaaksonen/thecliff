# Admin authentication
AuthType Basic
AuthName "Menu Administration"
AuthUserFile /path/to/.htpasswd
Require valid-user

# Security headers
Header always set X-Content-Type-Options nosniff
Header always set X-Frame-Options DENY 